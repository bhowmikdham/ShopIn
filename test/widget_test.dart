// Basic smoke test for ShopIn Flutter app.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('ShopIn sanity check', () {
    // Placeholder – Firebase requires initialisation so full widget
    // tests need a mock setup.  This ensures the test runner works.
    expect(1 + 1, equals(2));
  });
}
